#actions
  %ul
    %li= link_to "Add new list", "#"
#records
  %ul#sortable_list
    - @lists.each do |list|
      %li.clearfix.record{:class => cycle("on", "on-hover")}
        = link_to list["name"], refinery.mailchimp_admin_list_path(list["id"])
